{
  "name": "facechain-chain",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat workspace for the FaceProofRegistry contract (facechain stage 3)",
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "hardhat": "^2.22.17",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
